{
  "name": "clappr-architecture",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:web && npm run build:native",
    "build:web": "node build-scripts/build-web.cjs",
    "build:native": "node build-scripts/build-native.cjs",
    "start:web": "npm run build:web && npx serve dist/web",
    "start:native": "npm run build:native && node dist/native/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "live-server": "^1.2.2"
  }
}
